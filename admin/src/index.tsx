import React from 'react';
import { render } from 'react-dom';
import { Button } from '@firmanc/ui-shared';

const Admin = () => (
  <Button />
);

const container = document.getElementById('root');
render(<Admin />, container);
